# GET /sponsors/categories/:category_id/items

## Response

```
{
  itmes: [
    {
      id: INT,
      title: VARCHAR,
      description: TEXT,
      site_url: VARCHAR,
      thumbnail_url: VARCHAR,
    }
  ]
}
```
